# APEXONE Database Lifecycle

Install is enabled. Upgrade/uninstall are orchestrated separately. Rollback remains unsupported until all rollback paths are implemented and tested. Lifecycle scripts orchestrate source; they do not duplicate object definitions.
